[2026-04-17] 문제: NSTextView 기반 마크다운 에디터에서 `# 제목` + Enter 후 전체선택/삭제 시 heading 스타일이 다음 입력에 남음 → 해결: storage.length == 0일 때 typingAttributes를 기본값으로 리셋. NSTextView는 selection이 heading 영역을 커버하면 typingAttributes를 그 스타일로 동기화하고, 삭제 후에도 이를 보존하기 때문.
[2026-04-20] 문제: InputBarView paste 로직이 handlePasteKey + onChange 2곳에 분산. onChange 감지가 delta>1 + (isPasteInProgress || pbCount != lastPasteChangeCount || clipboardHasImageOrFile) 같은 휴리스틱에 의존해, 이미지 paste 후 한글 IME + space 커밋 시 delta>1 되면 같은 이미지가 중복 첨부되고 입력 텍스트가 롤백됨. → 해결: handlePasteKey를 단일 진입점으로 만들어 Cmd+V를 항상 .handled로 소비. clipboard 내용 우선순위(fileURL → image data → URL → long text → short text)로 분기. onChange 감지 로직, isPasteInProgress, lastPasteChangeCount, skipPasteDetection state 전부 제거. 교훈: paste 감지를 "텍스트 변경 delta" 같은 후행 휴리스틱으로 역추적하지 말고, 실제 paste 이벤트(Cmd+V / drag-drop)에서 직접 처리할 것.
[2026-04-20] 문제: Swift 6 동시성에서 NSItemProvider 콜백 클로저(nonisolated) 내부에서 MainActor 메서드에 provider를 인자로 전달하면 "sending risks causing data races" 에러. → 해결: 클로저 내부에서 provider를 재사용하는 경우 코드를 인라인하거나 provider를 지역 변수로만 사용. MainActor 메서드는 외부(isolation 경계를 넘지 않는 곳)에서만 호출.
[2026-04-20] 문제: assistant 텍스트 버블이 한 줄로 잘려 "..." 표시됨. MessageBubble.swift의 `assistantTextBubble`는 HStack(alignment: .bottom)으로 텍스트와 streaming cursor를 감싸는데, streaming Text에는 `.frame(maxWidth: .infinity, alignment: .leading)`이 있어 width 확장되지만 non-streaming의 MarkdownContentView에는 누락되어 있어 HStack이 intrinsic width로 측정되어 truncate됨. → 해결: MarkdownContentView에도 동일한 `.frame(maxWidth: .infinity, alignment: .leading)` 추가. 교훈: HStack의 자식 중 하나라도 width-expanding modifier가 빠지면 두 갈래(streaming/non-streaming)에서 layout 동작이 달라짐. 분기되는 뷰의 layout modifier는 동일하게 맞출 것.
[2026-04-20] 문제: PermissionModal의 `세션 허용` 버튼이 PermissionServer에서 `scopedAllows`(session 단위, 도구 무관) 키로 저장되어, 한 번 누르면 같은 세션의 모든 도구가 자동 승인 — plan 모드 보호도 무력화됨. → 해결: PermissionDecision을 `.allowSessionTool`(Edit류·세션 한정·메모리)와 `.allowAlwaysCommand`(Bash·프로젝트별 영구·`bash_allowlist.json`)로 분리. PermissionRequest에 `streamPermissionMode` 필드를 추가해 popup 시점이 아닌 stream 시작 시점의 모드를 캡처(picker 변경으로 인한 plan 가드 누수 방지). 모달은 Bash/그 외/plan 여부에 따라 버튼 분기. 교훈: 권한 승인 UX 설계 시 (1) scope을 도구·명령 단위로 좁혀 broad allow 누수 방지, (2) 사용자가 의도적으로 보호 모드(plan)를 켰다면 broad-scope 옵션은 숨기기, (3) 사용자 결정의 컨텍스트(stream의 mode)는 결정 시점이 아닌 트리거 시점에 캡처해 race 방지.
[2026-04-29] 실패한 시도(되돌림): user 메시지 텍스트 drag-selection이 잘 안 된다는 보고에 (1) `.contextMenu`를 부모 VStack→`Text` 자체로 이동 + (2) ScrollView `.onTapGesture`→`.simultaneousGesture(TapGesture)`로 변경했더니 drag가 **아예** 안 됨. 원상 복구. 교훈: (a) SwiftUI macOS에서 `Text` 자신에 `.contextMenu`+`.textSelection(.enabled)`을 함께 붙이면 long-press recognizer가 drag selection을 가로채는 알려진 충돌 — contextMenu는 부모 컨테이너에 두는 편이 안전. (b) 가설을 한 번에 두 개씩 바꾸지 말 것 — 무엇이 원인인지/악화 요인인지 분리되지 않음. Iron Law: 한 번에 한 변수만 바꾼다.

[2026-04-20] 문제: InputBarView의 Shift+Enter가 한글 IME 조합 중엔 안 먹히고, 소프트랩(줄바꿈 없이 width로 래핑)으로 줄이 늘어나도 TextField 높이가 안 자람. → 해결 (1): 일반 Enter의 `hasMarkedText → unmarkText → pendingSend` 패턴을 Shift+Enter에도 미러링 — `pendingNewline` flag 후 onChange에서 `Task { @MainActor in windowState.inputText.append("\n") }`. (2): `TextField(axis:.vertical) + .lineLimit(1...10)`은 macOS에서 하드 \n만 높이 측정하고 소프트랩은 누락. 숨겨진 `Text`를 GeometryReader 폭에 맞춰 렌더링해 래핑 높이를 측정 → `measuredInputHeight`로 `frame(minHeight:)` 구동. 빈 문자열/trailing \n은 " " 접미해 0높이 방지. 교훈: (a) SwiftUI 상위 텍스트 API가 IME 경계를 숨긴다 — IME가 관여하는 키 바인딩은 항상 `hasMarkedText()` 체크 후 `unmarkText()` + onChange 디퍼드 처리. (b) `TextField(axis:.vertical)`의 소프트랩 높이는 신뢰할 수 없음, 필요하면 hidden `Text` 측정 또는 NSTextView로 전환.

[2026-04-29] 문제: 터미널이나 메모(NSTextView)를 클릭해도 포커스가 유지되지 않고 메시지 입력창(IMETextView)으로 다시 빨려감. → 원인: `IMETextView.updateNSView`가 `isFocused == true`인 동안 매 SwiftUI re-render마다 `makeFirstResponder(textView)`를 호출. `InputBarView.isInputFocused`는 `@FocusState`가 아닌 단순 `@State Bool`이라 외부에서 firstResponder가 바뀌어도 false로 동기화되지 않음. 그래서 windowState/chatBridge 변화로 InputBarView가 re-render되면 그때마다 IMETextView가 firstResponder를 강탈. → 해결: `_IMETextView`에 `becomeFirstResponder()`/`resignFirstResponder()` 오버라이드를 추가하고 `onFocusChange` 콜백으로 `isFocused` 바인딩을 양방향 동기화. 사용자가 터미널/메모를 클릭해 IMETextView가 firstResponder를 잃으면 isFocused가 false가 되어 다음 update에서 강탈하지 않음. 교훈: NSViewRepresentable의 `isFocused` 같은 양방향 바인딩은 NSView 측에서도 firstResponder 변화를 바인딩으로 push해야 함. 단방향(SwiftUI→NSView)으로만 만들면 SwiftUI가 NSView 외부의 firstResponder 변경을 모르고 무한히 강탈한다.

[2026-04-29] 문제: 사이드바 inspector 탭(터미널/메모) 버튼을 클릭해 패널을 전환해도 chat input(IMETextView)이 firstResponder를 그대로 들고 있어 키보드 입력이 메모/터미널로 가지 않음. → 원인: SwiftUI `Button(.plain)` 클릭은 macOS에서 firstResponder를 가져가지 않음. `InspectorPanel`은 두 패널을 항상 마운트해두고 `frame(maxHeight:)` 0/.infinity로 토글만 할 뿐, 탭 변경 시 새 패널의 NSView를 firstResponder로 잡는 로직이 없었음. lessons 10번의 IMETextView fix는 "사용자가 영역을 직접 클릭"한 시나리오(B)만 해결 — 탭 버튼 클릭(A) 시나리오는 별개로 명시적 트리거 필요. → 해결: 코드베이스 기존 `clearTrigger: UUID?` 패턴을 그대로 차용해 `terminalFocusID`/`memoFocusID` UUID state를 `InspectorPanel`에 두고, `.onChange(of: inspectorTab)`에서 새 트리거 발급. `EmbeddedTerminalView`/`InspectorMemoPanel`(RichEditorView)이 `updateNSView`에서 `lastFocusTrigger` 비교 후 변경 시점에만 자기 NSView를 `makeFirstResponder`. 부수 효과로 chat input의 _IMETextView가 resignFirstResponder를 받아 isFocused=false로 동기화되어 강탈 루프도 함께 끊김. 교훈: SwiftUI `Button` 클릭은 firstResponder 이전을 보장하지 않음 — "탭 클릭 = 그 패널 입력으로 자동 포커스" UX는 trigger UUID + updateNSView 가드 패턴으로 명시적 makeFirstResponder를 호출해야 한다. 첫 마운트 시점엔 trigger=nil로 두어 자동 발화를 막을 것.

[2026-04-29] 문제: lessons 10·11 fix 후에도 (a) 메모 영역(특히 빈 공간/체크박스) 클릭 시 메모로 포커스 이동 안 됨, (b) 이미 활성인 탭을 다시 클릭하면 포커스 발화 안 됨, (c) 인스펙터 패널 토글로 다시 열어도 활성 탭 입력으로 포커스 안 옴. → 원인: (a) NSScrollView의 `clipView` 빈 영역에 떨어진 클릭은 documentView로 forwarding되지 않고 clipView가 흡수 → NSTextView.mouseDown 미발화 → first responder 이전 안 일어남. 또 `MemoTextView.mouseDown`의 체크박스 분기가 `super.mouseDown` 우회하고 즉시 return해 first responder 이전 누락. (b) `.onChange(of: inspectorTab)`은 값 변경시에만 fire → 동일 탭 재클릭은 no-op. (c) `showInspector` 토글에 대한 onChange 핸들러 자체가 없었음. → 해결: (a) `MemoScrollView: NSScrollView` 서브클래스에서 `hitTest`가 `contentView`를 반환할 때 `documentView`로 redirect — clipView 빈 영역 클릭이 NSTextView.mouseDown으로 흘러가 super가 first responder 가져감. 체크박스 분기엔 `window?.makeFirstResponder(self)`를 toggleCheckbox 직전에 명시 호출. (b) `InspectorTabControl`에 `onTabClick: (InspectorTab) -> Void` 콜백 파라미터 추가, 버튼 액션에서 `selection = tab` 직후 `onTabClick(tab)` 호출 — 동일 탭 재클릭에도 발화. (c) `InspectorPanel`에 `bumpFocus(for:)` 헬퍼로 focus trigger 발급을 통합하고 `.onChange(of: showInspector)`에서 isShowing이면 `bumpFocus(for: inspectorTab)`. 교훈: (1) NSScrollView + documentView 패턴에서 documentView가 visible area보다 작을 때 clipView 배경 클릭은 documentView로 자동 전달되지 않는다 — `hitTest` 오버라이드로 contentView→documentView redirect하거나 documentView 최소 높이를 clipView 크기에 맞춰야 함. (2) `mouseDown`을 오버라이드해 early return하는 분기는 super를 우회하므로 first responder 이전이 누락된다 — 모든 분기에서 명시적 `makeFirstResponder` 또는 super 호출 보장. (3) "탭 클릭 = 포커스" UX는 `onChange(of: tab)`만으로 부족 — 동일 값 재클릭도 트리거되도록 onClick 콜백을 별도로 둬야 한다. (4) "패널 열림 = 활성 탭 포커스"는 `onChange(of: showInspector)`로 별도 처리.

[2026-04-29] 문제: CLI session 사이드바 타이틀이 터미널에서 새 세션 만든 직후엔 `Session f28cb6e6`(sid prefix fallback)으로 박혀, CLI가 `ai-title` 라인을 jsonl에 추가해도 갱신 안 됨. → 원인: `DirectoryWatcher`가 `DispatchSource.makeFileSystemObjectSource`로 디렉토리 inode에 kqueue를 다는데, **macOS kqueue NOTE_WRITE/EXTEND는 디렉토리 엔트리 변동(파일 추가/삭제/리네임)에만 fire** — 기존 파일에 in-place append 해도 디렉토리 inode는 안 바뀌므로 이벤트 0건. CLI의 user/assistant/ai-title 라인 append는 전부 기존 jsonl에 쌓이는 추가 쓰기라 watcher가 평생 안 깨어남. → 해결: `DirectoryWatcher`를 FSEventStream + `kFSEventStreamCreateFlagFileEvents`로 교체. FSEventStream은 폴링 아닌 push(`fseventsd` 데몬이 커널 이벤트 추적), `FileEvents` 플래그가 켜지면 디렉토리 트리 내부 파일 단위 수정/생성/삭제까지 콜백. C 콜백 ↔ actor 브리지는 `Unmanaged.passRetained(StreamContext)`로 +1 retain leak 후 `unwatch`에서 release. fallback 타이틀 문구도 `"Session \(sid.prefix(8))"` → `"New Session"`(코드베이스 기존 컨벤션)으로 변경. 교훈: macOS에서 "디렉토리 안의 파일 변동까지 감시"는 **kqueue로 못 잡는다** — FSEventStream + FileEvents 플래그 필수. kqueue는 단일 fd가 가리키는 inode 자체의 이벤트만 본다.

[2026-04-29] 실패한 1차 진단(되돌림): 사용자가 "응답 중 다른 히스토리 클릭하면 메시지가 안 나온다"고 보고 → 트리거 누락으로 오진해 `MessageListView`에 `.onChange(of: chatBridge.messages.last?.id)` 트리거를 추가. 사용자 재확인 결과 메시지가 안 나오는 게 아니라 "예전엔 거의 즉시였는데 지금은 너무 느리게 나옴". 트리거는 정상 동작 중이었고 진짜 원인은 별개의 지연 문제였음. 교훈: 사용자가 "안 나온다"고 표현해도 실제는 "지연돼서 안 나오는 것처럼 보인다"일 수 있음 — 한 번 더 확인하기 전에 코드를 추론으로 패치하지 말 것. Iron Law: 재현 안 된 가설로 코드를 고치지 않는다.

[2026-04-29] 문제: 메시지 버블 텍스트 drag-selection이 안 됨, 깜빡임, 드래그 시작 자체 실패. 1f62609에서 추가한 `messageTextSelectionArea()`(부모에 contentShape+textSelection 중복) 제거, 양 버블 `.contextMenu` 제거(user는 hover overlay Copy/Edit으로 대체), `MessageListView` ScrollView `.onTapGesture` 제거까지 4단계 모두 효과 없음. → 진짜 원인: `IMETextView.updateNSView`가 `isFocused == true`일 때 매 SwiftUI re-render마다 `DispatchQueue.main.async { makeFirstResponder(textView) }`를 dispatch. 사용자가 메시지 텍스트에서 click drag을 시작하면 selection NSTextView가 잠깐 firstResponder를 가져가는데, 그 사이 이미 큐에 쌓인 async 블록이 stale `isFocused=true`로 firstResponder를 다시 강탈 → selection cancel + 깜빡임. lesson #10이 양방향 sync를 추가했지만 binding update의 비동기성 때문에 race 윈도우가 남아있었음. → 해결: `IMETextView`에 `focusTrigger: UUID?` 파라미터 추가. updateNSView에서 trigger UUID가 변경된 시점에만 makeFirstResponder. SwiftUI re-render만으로는 강탈하지 않음. `InputBarView`의 5곳 `isInputFocused = true` 외부 호출을 모두 `inputFocusTrigger = UUID()`로 교체. `isInputFocused` State는 IMETextView의 firstResponder 상태 양방향 sync 용도로만 유지(lesson #10 효과 보존). 교훈: (1) `@Binding Bool` 기반의 "focus를 주세요" 신호는 SwiftUI binding update의 비동기성 때문에 race 가능 — `DispatchQueue.main.async` 안에서 firstResponder 강탈하면 다른 view가 정당하게 잡은 firstResponder를 빼앗을 수 있음. trigger UUID 패턴(lesson #11/#12)으로 명시적 요청 시점에만 강탈해야 안전. (2) "selection이 안 되네" 디버깅에서 SwiftUI 레벨 modifier(.contextMenu, .onTapGesture, .contentShape)를 의심하는 게 자연스럽지만, 실제 root cause는 멀리 있는 NSViewRepresentable의 firstResponder 강탈 race일 수 있다 — Iron Law: 3+ fix 실패하면 architecture를 의심하고 더 큰 변수를 찾을 것. (3) `isInputFocused = true`처럼 외부에서 강제 set하는 Bool flag는 trigger UUID로 구조화하면 race를 원천 차단할 수 있다.

[2026-04-29] 문제: CLI 세션 sync 도입(d5b15d6) 이후 응답 스트리밍 중 사이드바 다른 히스토리를 클릭하면 메시지가 표시되기까지 체감 가능한 지연이 발생(이전엔 거의 즉시). → 원인: `CLISessionStore.loadFullSession`이 `for try await rawLine in url.lines` 패턴(AsyncBytes 기반 비동기 line iterator)으로 jsonl을 읽음. AsyncBytes는 byte-by-byte userland 파이프라인이라 작은 파일에서도 mmap+split 대비 ~한 자릿수 느림. 게다가 `cliStore`는 actor라 FSEventStream이 발화시킨 `loadSummaries` 호출들이 큐에 쌓여 있으면 그 뒤로 직렬화돼 더 늘어남. → 해결: `loadFullSession`에서 `Data(contentsOf: url, options: .mappedIfSafe)`로 일괄 읽기 → `String.split(separator: "\n", omittingEmptySubsequences: true)`로 라인 분할로 변경. append-only 보장은 그대로(마지막 라인이 부분 쓰여진 상태면 JSON decode 실패해서 silently skip). 교훈: 스트리밍성/이어 읽기가 필요 없는 "이미 끝난 파일"은 `url.lines`(AsyncBytes) 대신 `Data(contentsOf:)` + split이 압도적으로 빠름. 사용자 클릭이 직접 기다리는 경로(세션 전환 시 메시지 로드)에선 절대 AsyncBytes를 쓰지 말 것.
